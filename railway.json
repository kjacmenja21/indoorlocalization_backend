{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "sleep 5 && hypercorn main:app --bind \"[::]:$PORT\""
    }
}