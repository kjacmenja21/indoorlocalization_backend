{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "sleep 5 && hypercorn --workers 4 --worker-class asyncio  app:app --bind \"[::]:$PORT\""
    }
}